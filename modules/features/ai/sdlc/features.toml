[metadata]
name = "features"
description = "sdlc features in the sdlc-state repo"
requirements = ["sdlc"]

[source]
command = "sdlc list --path"
entry_delimiter = "\n"
display = "{split:\t:1} ({split:\t:2})"
output = "{split:\t:0}"

[preview]
command = "sdlc status {split:\t:1}"
