[metadata]
name = "repos"
description = "repositories under ~/code"
requirements = ["repos", "git"]

[source]
command = "repos list"
display = "{split:/:4..}"
output = "{}"

[preview]
command = "git -C '{}' log --oneline --decorate --graph -20 --color=always"
