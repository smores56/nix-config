[metadata]
name = "repos"
description = "ghq-managed repositories"
requirements = ["ghq", "git"]

[source]
command = "sh -c 'ghq list -p | while read -r p; do [ -d \"$p/.git\" ] && echo \"$p\"; done'"
display = "{split:/:4..}"
output = "{}"

[preview]
command = "git -C '{}' log --oneline --decorate --graph -20 --color=always"
